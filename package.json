{
  "name": "web-video-clip",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm --filter @web-video/clip-editor dev",
    "build": "pnpm --filter @web-video/clip-editor build",
    "start": "pnpm --filter @web-video/clip-editor dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
